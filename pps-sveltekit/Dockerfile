FROM node:20

WORKDIR /app
COPY . .

ARG DATABASE_URL
ARG VITE_MEDUSA_PUBLISHABLE_KEY
ARG VITE_MEDUSA_BACKEND_URL
# ENV DATABASE_URL=${DATABASE_URL:-file:local.db}

RUN npm install
RUN npm run build

CMD ["node", "build"]
